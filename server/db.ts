
// Dummy database object for API compatibility
export const db = {
  // This is now just a placeholder since we're using in-memory storage
  // All actual data operations are handled in storage.ts
};
